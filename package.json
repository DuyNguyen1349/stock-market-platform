{
  "name": "stock-market-platform",
  "version": "1.0.0",
  "description": "Full-Suite Stock Market Platform Analytics Dashboard",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:engine": "cd data_engine && python run.py",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:engine\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
